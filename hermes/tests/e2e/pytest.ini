[pytest]
# Test navigateur de la connexion OIDC (Playwright), piloté depuis l'hôte : lancé explicitement
# par .github/workflows/image.yml, jamais par la suite du poste (pytest.ini racine).
addopts = --strict-markers -p no:cacheprovider
