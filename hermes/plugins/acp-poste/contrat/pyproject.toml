[project]
name = "acp-poste-contrat"
version = "0.11.0"
description = "Contrat Python partagé par le poste Windows et le greffon Hermes acp-poste"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.7"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["acp_poste_contrat"]
