# Acp.Components — composants composites : états, pastilles, lignes de valeurs.

acp_add_qml_module(
    TARGET acp_components
    URI Acp.Components
    QML_FILES
        EmptyState.qml
        KeyValueRow.qml
        QuotaGauge.qml
        SectionHeader.qml
        StatusChip.qml
)
