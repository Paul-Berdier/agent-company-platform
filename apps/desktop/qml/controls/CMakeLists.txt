# Acp.Controls — contrôles de base de la station.

acp_add_qml_module(
    TARGET acp_controls
    URI Acp.Controls
    QML_FILES
        AcpButton.qml
        AcpIconButton.qml
        AcpTextField.qml
)
